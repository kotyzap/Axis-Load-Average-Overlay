# Axis-Load-Average-Overlay
Turn your Axis camera's own health data into a live, color-coded overlay. Axis Load Average Overlay is a lightweight CamScripter microapp that reads your Axis camera's CPU load and pushes it straight onto the video feed via CamOverlay — no dashboards, no agents, no extra infrastructure.
